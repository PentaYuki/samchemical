{
  "name": "samchemical-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c \"node build.cjs\"",
    "build": "node build.cjs",
    "build:tina": "tinacms build && node build.cjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "tinacms": "latest",
    "@tinacms/cli": "latest"
  }
}